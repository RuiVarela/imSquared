{
    "version": "2.0.0",
    "tasks": [
        {
            "type": "shell",
            "label": "build imSquared",
            "command": "make",
            "args": [
                "imSquared"
            ],
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "problemMatcher": [
                "$gcc"
            ],
            "group": "build"
        }
    ]
}